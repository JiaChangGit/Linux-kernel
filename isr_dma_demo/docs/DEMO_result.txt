──── Step 1: Load Kernel Module ────
[INFO]  Loading isr_dma_module.ko ...
[OK]    Module loaded successfully.

Kernel log (module init):
  [ 4553.370418] isr_dma: loading — ring slots=256 slot_size=64 total=20480
  [ 4553.408361] isr_dma: loaded — /dev/isr_dma ready, /proc/isr_dma_stats ready
  [ 4553.408374] isr_dma: ring @ virt=00000000364169aa dma=0x00000000011d8000 size=20480

──── Step 2: Device Node Verification ────
[OK]    /dev/isr_dma device node created by udev.
  crw------- 1 root root 239, 0  5月 11 20:45 /dev/isr_dma

──── Step 3: Observe ISR Firing via /proc/isr_dma_stats ────
[INFO]  Opening device to start ISR timer, then watching /proc for 3 seconds...

--- /proc/isr_dma_stats @ t=1s ---
  === ISR + DMA Ring Buffer — Kernel Stats ===
  isr_count       : 14
  drop_count      : 0
  ring_head       : 14
  ring_tail       : 0
  
  --- Benchmark (per-operation average) ---
  isr_dma_ops     : 14
  isr_dma_avg_ns  : 261
  naive_ops       : 14
  naive_avg_ns    : 513
  speedup_x       : 1.96
--- /proc/isr_dma_stats @ t=2s ---
  === ISR + DMA Ring Buffer — Kernel Stats ===
  isr_count       : 1442
  drop_count      : 1187
  ring_head       : 255
  ring_tail       : 0
  
  --- Benchmark (per-operation average) ---
  isr_dma_ops     : 255
  isr_dma_avg_ns  : 300
  naive_ops       : 1442
  naive_avg_ns    : 423
  speedup_x       : 1.41
--- /proc/isr_dma_stats @ t=3s ---
  === ISR + DMA Ring Buffer — Kernel Stats ===
  isr_count       : 2325
  drop_count      : 2070
  ring_head       : 255
  ring_tail       : 0
  
  --- Benchmark (per-operation average) ---
  isr_dma_ops     : 255
  isr_dma_avg_ns  : 300
  naive_ops       : 2325
  naive_avg_ns    : 438
  speedup_x       : 1.46

Final kernel stats after 3s:
  === ISR + DMA Ring Buffer — Kernel Stats ===
  isr_count       : 3631
  drop_count      : 3376
  ring_head       : 255
  ring_tail       : 0
  
  --- Benchmark (per-operation average) ---
  isr_dma_ops     : 255
  isr_dma_avg_ns  : 300
  naive_ops       : 3631
  naive_avg_ns    : 444
  speedup_x       : 1.48

──── Step 4: Ring Buffer Architecture Visualisation ────

  ┌─────────────────────────────────────────────────────────┐
  │               DMA-Coherent Ring Buffer                  │
  │  (physically contiguous, cache-coherent, shared memory) │
  │                                                         │
  │  ┌───────┬───────┬───────┬─────┬───────┬───────┐       │
  │  │ slot0 │ slot1 │ slot2 │ ... │N-2    │N-1    │       │
  │  │64 B   │64 B   │64 B   │     │64 B   │64 B   │       │
  │  └───────┴───────┴───────┴─────┴───────┴───────┘       │
  │       ▲                              ▲                  │
  │     TAIL (consumer)               HEAD (ISR producer)  │
  │                                                         │
  │  Control Block:  head=atomic32  tail=atomic32           │
  │                  isr_count      drop_count              │
  └─────────────────────────────────────────────────────────┘

  Producer (ISR/hrtimer):                                   
    1. Check ring_full(head, tail)                          
    2. Write 64-byte payload to data[head * SLOT_SIZE]      
    3. Atomic advance of head (release barrier)             

  Consumer (userspace):                                     
    Path A — mmap (zero-copy):                              
      1. Read data[tail * SLOT_SIZE] directly from mapping  
      2. Atomic advance of tail (acquire barrier)           
    Path B — read() syscall:                                
      1. Kernel reads slot, copy_to_user() to userspace buf 


──── Step 5: Quick Functional Read Demo ────
[INFO]  Reading 5 slots from /dev/isr_dma via hexdump...

  00000000  65 0e 3f 84 25 04 00 00  01 00 00 00 00 00 00 00  |e.?.%...........|
  00000010  ab ab ab ab ab ab ab ab  ab ab ab ab ab ab ab ab  |................|
  *
  00000040  be 55 68 84 25 04 00 00  02 00 00 00 00 00 00 00  |.Uh.%...........|
  00000050  ab ab ab ab ab ab ab ab  ab ab ab ab ab ab ab ab  |................|
  *
  00000080  8d 46 92 84 25 04 00 00  03 00 00 00 00 00 00 00  |.F..%...........|
  00000090  ab ab ab ab ab ab ab ab  ab ab ab ab ab ab ab ab  |................|
  *
  000000c0  be 83 ac 84 25 04 00 00  04 00 00 00 00 00 00 00  |....%...........|
  000000d0  ab ab ab ab ab ab ab ab  ab ab ab ab ab ab ab ab  |................|
  *
  00000100  17 55 cc 84 25 04 00 00  05 00 00 00 00 00 00 00  |.U..%...........|
  00000110  ab ab ab ab ab ab ab ab  ab ab ab ab ab ab ab ab  |................|
  *
  00000140

[INFO]  Each 64-byte slot layout:
  Bytes  0- 7:  64-bit kernel timestamp (ns)
  Bytes  8-15:  ISR invocation counter
  Bytes 16-63:  0xAB pattern fill

──── Step 6: Kernel Log Summary ────
Recent dmesg from isr_dma:
  [ 4553.370418] isr_dma: loading — ring slots=256 slot_size=64 total=20480
  [ 4553.408361] isr_dma: loaded — /dev/isr_dma ready, /proc/isr_dma_stats ready
  [ 4553.408374] isr_dma: ring @ virt=00000000364169aa dma=0x00000000011d8000 size=20480
  [ 4555.529673] isr_dma: timer/ISR started (500000 ns period)
  [ 4558.848458] isr_dma: timer/ISR stopped. isr_count=3631 drops=3376
  [ 4558.965791] isr_dma: timer/ISR started (500000 ns period)
  [ 4559.270895] isr_dma: timer/ISR stopped. isr_count=284 drops=29

════════════════════════════════════════════════
  Demo complete!
  Module is still loaded. Next step:
  sudo bash scripts/03_benchmark.sh
════════════════════════════════════════════════
louis@louis-VirtualBox:~/桌面/Linux-kernel/isr_dma_demo$ sudo bash scripts/03_benchmark.sh 

  ╔══════════════════════════════════════════════════════╗
  ║   ISR + DMA Ring Buffer — Benchmark                 ║
  ╚══════════════════════════════════════════════════════╝

[INFO]  Running benchmark (5s per path)...
[INFO]    Path A: mmap zero-copy  (DMA-coherent shared memory)
[INFO]    Path B: read() syscall  (kernel copy_to_user)


[consumer] Opening /dev/isr_dma ...
[consumer] Device opened. ISR timer running at 2000 IRQ/s.
[consumer] Each slot: 64 bytes, ring depth: 256 slots.

=== Run A: mmap zero-copy path (5 s) ===
mmap: Invalid argument
=== Run B: read() syscall path (5 s) ===

╔══════════════════════════════════════════════════════╗
║          ISR + DMA Ring Buffer  Benchmark           ║
╚══════════════════════════════════════════════════════╝

  Test: mmap vs read() on /dev/isr_dma

  Path                         Avg lat (ns)     Total ops
  ──────────────────────────────────────────────────
  A) ISR+DMA mmap (zero-copy)           0.0  140735203397424
  B) read() syscall (copy)          14056.0          3914

  Speedup  (B / A): 0.00x

  Throughput:
    read  path: ~71143 Mops/s

  ASCII Bar Chart (lower = faster):

  Note: 'zero-copy' avoids copy_to_user() — ISR writes
        directly to DMA-coherent memory visible from user.

  A) mmap (zero-copy) [........................................]     0.00 ns/op
  B) read() syscall  [########################################] 14056.00 ns/op
[consumer] Results saved to bench_results.txt

════ Kernel-Side Stats (from /proc) ════
  === ISR + DMA Ring Buffer — Kernel Stats ===
  isr_count       : 4949
  drop_count      : 1032
  ring_head       : 77
  ring_tail       : 74
  
  --- Benchmark (per-operation average) ---
  isr_dma_ops     : 3917
  isr_dma_avg_ns  : 284
  naive_ops       : 4949
  naive_avg_ns    : 488
  speedup_x       : 1.71

════ Benchmark Results ════

  Access Path                       Avg latency(ns)     Total ops
  ──────────────────────────────────────────────────────────────
  A) ISR+DMA mmap (zero-copy)                    0  140735203397424
  B) read() syscall (copy)                   14056          3914

  Speedup (B ÷ A): 0.00×

════ ASCII Bar Chart (lower = faster) ════

  A) mmap (zero-copy)          [░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░]      0 ns/op
  B) read() syscall            [██████████████████████████████████████████████████]  14056 ns/op

════ Interpretation ════

  Why is Path A (mmap/DMA) faster?
  ─────────────────────────────────
  • Path A avoids copy_to_user() entirely. The ISR writes directly into
    DMA-coherent memory that is already mapped into userspace — no kernel
    bounce buffer, no page fault, no TLB shootdown.

  • Path B (read syscall) crosses the kernel/user boundary on every slot:
    the kernel must validate the userspace pointer, call copy_to_user(),
    and return through the syscall path — adding hundreds of nanoseconds
    per operation.

  • In real embedded / real-time systems (network cards, audio DMA,
    frame grabbers), this difference translates to measurable latency
    reduction and lower CPU utilisation.

  Ring Buffer benefits over a plain buffer:
  ──────────────────────────────────────────
  • Lock-free producer/consumer (using atomic head/tail pointers).
  • ISR can write while consumer reads — no global lock needed.
  • Fixed memory footprint — no dynamic allocation in interrupt context.

    read path : ~71143 Mops/s (per-core)

════════════════════════════════════════════════
  Benchmark complete!
  Next step: sudo bash scripts/04_cleanup.sh
════════════════════════════════════════════════
